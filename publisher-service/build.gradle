dependencies {
  compile project(":modules:env"), project(":modules:log"),
      "io.vertx:vertx-web:$vertxVersion"
}

jar {
  manifest {
    attributes "Main-Class": "io.vertx.core.Launcher"
    attributes "Main-Verticle": "daggerok.PublisherService"
  }
}
